version: 0.2

env:
  shell: bash

phases:
  install:
    commands:
      - chmod +x ./scripts/bootstrap-argocd.sh ./scripts/pipeline-common/init-terraform-backend.sh ./scripts/pipeline-common/bootstrap-argocd-wrapper.sh

  build:
    on-failure: RETRY-1
    commands:
      - ./scripts/buildspec/bootstrap-argocd-rc.sh

artifacts:
  files: []
