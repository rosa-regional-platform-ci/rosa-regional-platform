version: 0.2

env:
  shell: bash

phases:
  build:
    on-failure: RETRY-1
    commands:
      - ./scripts/buildspec/provision-infra-mc.sh
artifacts:
  files:
    - '**/*'
  exclude-paths:
    - '**/.terraform/**'
    - '**/.terraform.lock.hcl'
    - '**/node_modules/**'
