apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-tmp-alb
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  tmp-alb.json: {{ .Files.Get "dashboards/infrastructure/tmp-alb.json" | quote }}
