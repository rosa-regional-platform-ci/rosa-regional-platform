apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-rc-health
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "rc"
data:
  rc-health.json: {{ .Files.Get "dashboards/rc/rc-health.json" | quote }}
