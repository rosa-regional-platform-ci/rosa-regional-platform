apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-tmp-dynamodb
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  tmp-dynamodb.json: {{ .Files.Get "dashboards/infrastructure/tmp-dynamodb.json" | quote }}
