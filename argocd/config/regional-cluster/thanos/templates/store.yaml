{{- if .Values.thanos.storeGateway.enabled }}
apiVersion: monitoring.thanos.io/v1alpha1
kind: ThanosStore
metadata:
  name: thanos-store
  namespace: {{ include "thanos-operator.namespace" . }}
  labels:
    {{- include "thanos-operator.labels" . | nindent 4 }}
  annotations:
    {{- include "thanos-operator.annotations" . | nindent 4 }}
spec:
  replicas: {{ .Values.thanos.storeGateway.replicas }}
  resourceRequirements:
    requests:
      cpu: {{ .Values.thanos.storeGateway.resources.requests.cpu }}
      memory: {{ .Values.thanos.storeGateway.resources.requests.memory }}
    limits:
      memory: {{ .Values.thanos.storeGateway.resources.limits.memory }}
  objectStorageConfig:
    name: {{ .Values.objectStore.secretName }}
    key: objstore.yml
  shardingStrategy:
    type: block
  storage:
    size: {{ .Values.thanos.storeGateway.storage.size }}
    {{- if .Values.thanos.storeGateway.storage.storageClass }}
    storageClass: {{ .Values.thanos.storeGateway.storage.storageClass }}
    {{- end }}
{{- end }}
