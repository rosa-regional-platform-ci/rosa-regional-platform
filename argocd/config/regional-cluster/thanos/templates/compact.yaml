{{- if .Values.thanos.compactor.enabled }}
apiVersion: monitoring.thanos.io/v1alpha1
kind: ThanosCompact
metadata:
  name: thanos-compact
  namespace: {{ include "thanos-operator.namespace" . }}
  labels:
    {{- include "thanos-operator.labels" . | nindent 4 }}
  annotations:
    {{- include "thanos-operator.annotations" . | nindent 4 }}
spec:
  resourceRequirements:
    requests:
      cpu: {{ .Values.thanos.compactor.resources.requests.cpu }}
      memory: {{ .Values.thanos.compactor.resources.requests.memory }}
    limits:
      memory: {{ .Values.thanos.compactor.resources.limits.memory }}
  objectStorageConfig:
    name: {{ .Values.objectStore.secretName }}
    key: objstore.yml
  retentionConfig:
    raw: {{ .Values.thanos.compactor.retentionResolutionRaw }}
    fiveMinutes: {{ .Values.thanos.compactor.retentionResolution5m }}
    oneHour: {{ .Values.thanos.compactor.retentionResolution1h }}
  storage:
    size: {{ .Values.thanos.compactor.storage.size }}
    {{- if .Values.thanos.compactor.storage.storageClass }}
    storageClass: {{ .Values.thanos.compactor.storage.storageClass }}
    {{- end }}
{{- end }}
