{{- if .Values.thanos.ruler.enabled }}
apiVersion: monitoring.thanos.io/v1alpha1
kind: ThanosRuler
metadata:
  name: thanos-ruler
  namespace: {{ include "thanos-operator.namespace" . }}
  labels:
    {{- include "thanos-operator.labels" . | nindent 4 }}
  annotations:
    {{- include "thanos-operator.annotations" . | nindent 4 }}
spec:
  replicas: {{ .Values.thanos.ruler.replicas | default 2 }}
  queryLabelSelector:
    matchLabels:
      app.kubernetes.io/name: thanos-query
  ruleConfigSelector:
    matchLabels:
      app.kubernetes.io/name: alerting-rules
  alertmanagerURL: {{ .Values.thanos.ruler.alertmanagerURL | quote }}
  alertLabelDrop:
    - rule_replica
  objectStorageConfig:
    name: {{ .Values.objectStore.secretName }}
    key: objstore.yml
  evaluationInterval: {{ .Values.thanos.ruler.evaluationInterval | default "1m" }}
  retention: {{ .Values.thanos.ruler.retention | default "48h" }}
  externalLabels:
    rule_replica: "$(NAME)"
    region: {{ .Values.global.aws_region | quote }}
  resourceRequirements:
    requests:
      cpu: {{ .Values.thanos.ruler.resources.requests.cpu }}
      memory: {{ .Values.thanos.ruler.resources.requests.memory }}
    limits:
      memory: {{ .Values.thanos.ruler.resources.limits.memory }}
  storage:
    size: {{ .Values.thanos.ruler.storage.size }}
    {{- if .Values.thanos.ruler.storage.storageClass }}
    storageClass: {{ .Values.thanos.ruler.storage.storageClass }}
    {{- end }}
{{- end }}
