{{- if .Values.global.cluster_name }}
apiVersion: v1
kind: Secret
metadata:
  name: loki-storage-config
  namespace: {{ include "loki.namespace" . }}
  labels:
    {{- include "loki.labels" . | nindent 4 }}
  annotations:
    {{- include "loki.annotations" . | nindent 4 }}
type: Opaque
stringData:
  bucketnames: {{ printf "%s-loki-logs-%s" .Values.global.cluster_name .Values.global.aws_account_id | quote }}
  # TODO: revisit FIPS endpoint (s3-fips) once loki-operator supports it.
  # Current operator validates endpoint must match https://s3.<region>.amazonaws.com
  endpoint: {{ printf "https://s3.%s.amazonaws.com" .Values.global.aws_region | quote }}
  region: {{ .Values.global.aws_region | quote }}
  {{- if .Values.loki.kmsKeyArn }}
  sse_type: "SSE-KMS"
  sse_kms_key_id: {{ .Values.loki.kmsKeyArn | quote }}
  {{- end }}
{{- end }}
